services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: saarthi-api
    env_file:
      - .env
    environment:
      - PORT=8080
    ports:
      - "8080:8080"
    restart: unless-stopped
