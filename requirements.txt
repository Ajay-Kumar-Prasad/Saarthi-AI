# Saarthi AI — Learning Agent dependencies

# Core ADK framework
google-adk==0.4.0

# API framework
fastapi==0.111.0
uvicorn==0.29.0

# AlloyDB — IAM-based connector (no passwords in code)
google-cloud-alloydb-connector==1.0.0
asyncpg==0.29.0

# Google Auth & Secret Manager
google-auth==2.29.0
google-cloud-secret-manager==2.20.0

# Vertex AI — Gemini model + embeddings
google-cloud-aiplatform==1.51.0
vertexai==1.51.0

# HTTP client for MCP tool calls
httpx==0.27.0

# Data validation
pydantic==2.7.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6